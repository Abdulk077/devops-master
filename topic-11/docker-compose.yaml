version: '3.8'

services:
  app:
    build: .
    networks:
      - mynet

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
    networks:
      - mynet

networks:
  mynet: